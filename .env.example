# No secrets required for local training/eval — everything runs against
# open-weight models and local files. Populate this only if you wire in
# a hosted LLM-judge scorer (e.g. an Anthropic API key) later.
# ANTHROPIC_API_KEY=
