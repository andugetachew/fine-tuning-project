__pycache__/
*.pyc
.env
venv/
.venv/

# Model weights and checkpoints are large and reproducible from training runs
models/checkpoints/
*.safetensors
*.bin

# HF cache if ever run locally
.cache/

# Colab/notebook checkpoints
.ipynb_checkpoints/

# Raw dataset stays out of git only if it contains anything sensitive;
# processed splits are derived and reproducible via prepare_dataset.py
data/processed/
