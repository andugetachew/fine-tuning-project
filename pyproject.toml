[project]
name = "backend-engineering-assistant"
version = "0.1.0"
description = "QLoRA fine-tuned instruct model for practical Python/Django backend engineering Q&A, served via FastAPI."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]